spring:
  liquibase:
    enabled: false
  datasource:
#    url: jdbc:h2:file:./db
    url: jdbc:h2:mem:test
    username: u
    password: 1
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
