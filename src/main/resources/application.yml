spring:
  liquibase:
    enabled: false
  datasource:
    url: jdbc:h2:file:./db
    username: u
    password: 1
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
